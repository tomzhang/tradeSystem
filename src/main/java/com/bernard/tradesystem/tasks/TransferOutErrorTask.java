package com.bernard.tradesystem.tasks;

import org.apache.log4j.Logger;

import java.util.concurrent.Callable;

public class TransferOutErrorTask implements Callable {
    private static Logger logger = Logger.getLogger(TransferOutErrorTask.class);

    @Override
    public Object call() throws Exception {

        return null;
    }
}
